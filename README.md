# nkf — NongKept web app (static front-end)

หน้าเว็บ static ของแอป NongKept (จังหวะรีวิวรายการจากอีเมลธนาคาร)
- ตัวหน้านี้ = UI ล้วน ๆ ไม่มีข้อมูล/ความลับ — ข้อมูลโหลดจาก backend (Google Apps Script) แบบ JSONP ตอนเปิดแอป
- การเข้าถึงข้อมูล = ต้องมี token ใน URL (`#k=...`) เท่านั้น
